# Claude Code Execution Authorization Template

> Inactive template. Do not treat this file's existence as authorization.
> Replace the bracketed plan reference only after reviewing Claude's Plan-mode
> output, then deliberately send the completed prompt as a new current message.

```text
Use `/Users/noir/Documents/amordle-final` as the project workspace and treat `[PASTE THE EXACT ACCEPTED PLAN OR ITS TRACKED PATH HERE]` as the approved implementation plan.

Implement that plan autonomously and faithfully. First revalidate the repository, branch, handoff tag, accepted v6.6 rollback point, GitHub, Vercel, Supabase, migration, bootstrap, parity, multiplayer-audit, and application-interface identities. Stop on meaningful drift or an authority contradiction.

Preserve AGENTS.md and every bootstrap contract. Work only on `codex/amordle-terminal-greenfield-implementation-2026-07-27` unless I separately authorize a different branch/worktree. Do not inspect Git stash, force-push, rewrite history, merge, change the default branch, release or mutate Production, modify the locked BRRRDLE-DEV shell, delete real users/data, expose secrets/private data/answers, enable a paid capability, or add a vendor.

Write the accepted plan into the Claude handoff package, update `CONTINUITY-LOG.md` at cohesive checkpoints, make normal private commits and non-force pushes, implement and test every mandatory ANNOT-01 through ANNOT-12 requirement and every additional finding explicitly included in the accepted plan, run the complete local acceptance stack, deploy only the exact green commit to a protected Preview, run bounded disposable-user hosted acceptance, register and clean exact resources with zero residue, reconcile parity/run-state/evidence, produce paired Markdown/script-free HTML reports and checklists, and create a final private golden checkpoint for my manual review.

Any new migration, Supabase function/service interface, provider setting, paid capability, destructive ambiguity, Production mutation, or unresolved secret/private-data/answer risk requires a separate exact decision packet and my authorization before mutation. Do not merge or release Production. Continue through ordinary implementation and test repair without stopping unless a stated stop condition is reached.
```
